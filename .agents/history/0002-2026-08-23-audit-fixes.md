# 0002: Исправление находок аудита (CRITICAL + HIGH + MEDIUM)

- **Дата/время (локальное):** 2026-08-23 03:40
- **Агент:** ox-alpha
- **Инициатор:** «да» (согласие на план исправлений из записи 0001)
- **Коммит на момент начала:** ea2bbbd + незакоммиченные правки в рамках этой задачи

## Что сделано

17 файлов изменено, 5 новых, 4 миграции. Детали:

### CRITICAL
- `core/views.py`: NameError устранён (`timezone.localdate()`), Streak через
  get_or_create; удалены неиспользуемые импорты.
- `hangul/views.py` (TTS): rate limit 30/мин на IP через Django cache вместо сессии,
  атомарная запись mp3 (tempfile + os.replace), 502 при сбое gTTS, `@require_GET`.
- Новая команда `hangul/management/commands/cleanup_tts.py` (--days=7, --dry-run).
- `api/serializers.py`: AnswerSerializer без `is_correct`/`explanation`.

### HIGH
- Новый `review/services.py`: единая SRS-логика `apply_review()` — atomic +
  select_for_update; «again» сбрасывает лестницу и ставит next_review +10 мин;
  достижения проверяются после save. Его используют review_page, flashcard_mode
  и API ReviewViewSet.create.
- `review/views.py`: POST отвечает JSON `{success, action, learned, next_review,
  pending_count}` для AJAX; невалидный action/слово → 400/404.
- `quiz/views.py` переписан: серверный таймер (старт в сессии при GET detail,
  просрочка > 60с грейса → попытка не засчитывается), atomic на submit,
  writing-ответы сравниваются нормализованным текстом (числа больше не ломаются),
  prefetch answers.
- `library/models.py`: UniqueConstraint Bookmark(user,language,slug) и
  Highlight(user,language,slug,text,start,end); toggle'и race-safe через IntegrityError.
- `library/pages.py`: кэш парса учебника 600с + logging битого frontmatter.
- N+1: select_related/annotate в vocabulary, review, progress, api
  (Category/Course/GrammarTopic/Quiz ViewSet'ы).

### MEDIUM
- `timezone.localdate()` в progress/accounts/core.middleware (границы дня по Ташкенту).
- DRF LibraryViewSet: language из запроса (ko/ja, иначе 400), required-поля → 400,
  bookmarks идемпотентны (201/200), notes требуют content, tags get_or_create.
- `vocabulary/views.py::add_to_review`: битый JSON → 400, строгий mark_learned,
  сообщение для no-op ветки.
- Модели: Word.Meta.ordering=['korean']; индекс UserWordProgress(user,learned,next_review).
- settings: RuntimeError при DEBUG=False без DJANGO_SECRET_KEY; убран no-op
  SECURE_BROWSER_XSS_FILTER; BrowsableAPIRenderer только при DEBUG.
- Мёртвый код: word_bank_json, questions_json(частично), IsOwner, quiz_dates.

### Миграции
- library 0007 (constraints), quiz 0002 (Answer ordering — дрейф со старого коммита),
  vocabulary 0002 (Word ordering), progress 0004 (индекс).

## Проверка

- Тесты: pytest → 89 passed (после каждого блока правок).
- `makemigrations --check` → чисто; migrate применён на dev db.
- Ручная проверка: GET `/` с словом в БД → 200 (раньше был бы 500).
- cleanup_tts --dry-run: нашёл 3 файла, отработал корректно.
- Остаточные warnings пагинации DRF устранены явным order_by в аннотированных ViewSet'ах.

## Что осталось / передано дальше

LOW-хвост зафиксирован в `.agents/known-issues.md`. Изменения НЕ закоммичены
(ждёт решения пользователя).
