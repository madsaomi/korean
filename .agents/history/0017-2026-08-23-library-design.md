# 0017: Дизайн-проход library + фикс title-баги

- **Дата/время (локальное):** 2026-08-23 11:30
- **Агент:** ox-alpha
- **Инициатор:** «продолжай улучшать и обновлять»
- **Коммит на момент начала:** fe24ede

## Что сделано

### Бага от массовой замены 0011 (4d06d9f)
Иконки `<i class="bi bi-*">` попали внутрь `{% block title %}` в 4 шаблонах
library → невалидный HTML в `<head>` (иконка отображалась в заголовке вкладки
как текст). Все 4 очищены; глобальный скан `block title` + `bi bi-` по всем
шаблонам → 0.

### Library UI
- index.html: тулбар 🎲/🔖/🎨/⏱/📭 → bi-dice-5/bookmark-fill/brush/stopwatch/inbox;
  бейдж «прочитано» ✓ → badge-pass + bi-check-lg.
- detail.html: кнопка закладки — статичная иконка bi-bookmark-fill вместо
  двух одинаковых 🔖 в обеих ветках if; «Выделения» → bi-brush; stopwatch;
  ✨ в подсказке выделений → bi-stars.
- home.html: muted.
- Флаги 🇰🇷🇯🇵 сознательно оставлены — маркеры языков в UI переключателя.

## Проверка

- pytest → 118 passed; скан дублированных class-атрибутов library → 0.
- Коммиты: 4d06d9f.

## Что осталось / передано дальше

Внутри library bookmarks/highlights/reader осталось ~15 эмодзи второго плана —
низкий приоритет. Основные разделы покрыты дизайн-системой.
