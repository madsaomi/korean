# 0001: Полный аудит проекта

- **Дата/время (локальное):** 2026-08-23 02:30
- **Агент:** ox-alpha (opencode CLI, модель x-preview-f-free) + 3 параллельных explore-субагента
- **Инициатор:** «сделай аудит полный»
- **Коммит на момент начала:** ea2bbbd "fix: resolve code audit findings"

## Что сделано

Аудит всех 11 приложений, 50 шаблонов, settings, тестов и миграций.
Проверка выполнялась параллельно: субагенты по группам apps
(vocabulary/review/progress; quiz/lessons/grammar/hangul; library/api),
лично — accounts/core/settings/middleware.

Итог аудита:
- **CRITICAL (3):** NameError `now` в `core/views.py:51` (500 главной при наличии слов);
  TTS-эндпоинт без защиты (обход лимита через cookie + некуда не деваются mp3);
  утечка `is_correct`/`explanation` квизов через API для анонимов.
- **HIGH (8):** битый AJAX-ответ review_page; клиентский таймер квиза;
  дефект SRS-лестницы («again» раздувал интервалы); отсутствие atomic/блокировок;
  Bookmark/Highlight без UniqueConstraint; перечитывание всех md учебника на каждый
  запрос; N+1 в 4 местах; небезопасные дефолты DEBUG/SECRET_KEY.
- **MEDIUM (10)** и **LOW (~15)** — таймзоны UTC/локаль, writing-ответы квиза,
  language в DRF library, гонки Streak, мёртвый код и пр.

Полный отчёт был выдан пользователю в чате (не сохранён в файлы).

## Проверка

- Тесты до правок: pytest → 89 passed.
- Обнаружено: `makemigrations --check` падал (дрейф quiz.Answer.Meta.ordering).
- Окружение: venv отсутствовал — создан, зависимости установлены.

## Что осталось / передано дальше

Все находки переданы в задачу 0002 (исправления); остаток LOW — см. known-issues.md.
