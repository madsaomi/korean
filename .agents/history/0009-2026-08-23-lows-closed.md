# 0009: Закрыт весь LOW-хвост аудита

- **Дата/время (локальное):** 2026-08-23 06:00
- **Агент:** ox-alpha
- **Инициатор:** «продолжай» (очередь из HANDOFF)
- **Коммит на момент начала:** 019e2bc

## Что сделано

### Индексы (known-issues #1)
- `library/models.py`: Index(user, language, slug) на Bookmark, Highlight, Note;
  миграция library 0008.

### Гонки grammar-упражнений (known-issues #2)
- `start_exercise` → `@require_POST`, форма в exercise_list.html переведена на POST
  (GET больше не сбрасывает прохождение предзагрузкой страницы).
- Двойной сабмит: guard `grammar_last_answered_index` — повторный POST той же
  позиции не считается второй раз.
- `exercise_result` снапшотит и очищает grammar_* ключи сессии.

### Антифарминг (known-issues #3)
- Квиз: кулдаун 30 сек между попытками одного квиза (RESUBMIT_COOLDOWN_SECONDS),
  иначе warning + redirect.
- SRS: `apply_review` бросает WordNotDue (ValueError), если слово ещё не due
  (грейс DUE_GRACE_SECONDS=5). HTML-эндпоинты отдают 400, API — 400 с текстом.

### Сериализаторы (known-issues #4)
- Все 16 `fields='__all__'` заменены явными списками полей в api/serializers.py.
  Массовое назначение теперь невозможно по построению.

### Инфраструктура
- Token endpoint подключён: POST /api/token-auth/ (obtain_auth_token).
- LOGGING в settings: root console INFO (env DJANGO_LOG_LEVEL), django.request ERROR.

### Тесты (+8, всего 115)
- `lessons/tests.py` (был пустым): модели Course/Lesson/LessonStep + вьюхи
  (список публичный, detail требует логина).
- review/tests.py: not-due слово отклоняется; quiz — существующие таймер-тесты
  прошли без изменений.

## Проверка

- pytest → 115 passed; ruff → чисто; makemigrations --check → чисто.
- Попутно поймал и исправил 2 ошибки в собственных новых тестах
  (незапрошенная фикстура lesson; не-due слово в тесте again).

## Что осталось / передано дальше

Аудит закрыт полностью. known-issues.md переписан: остались только осознанные
компромиссы и прод-заметки (Redis, .env-loader, PostgreSQL, media-раздача).
