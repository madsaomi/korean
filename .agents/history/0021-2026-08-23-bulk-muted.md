# 0021: Bulk muted/badge — 25 шаблонов

- **Дата/время (локальное):** 2026-08-23 14:45
- **Агент:** ox-alpha
- **Инициатор:** «продолжай» (системный inline-долг)
- **Коммит на момент начала:** 0d37846

## Что сделано

Массовый проход по инлайн-стилям (самый частый паттерн `color: var(--text-secondary)`):
- Скрипт с 2 порядками `class+style` / `style+class` → `.muted` + `badge-accent`.
- 25 шаблонов, ~50 инлайнов удалено; после мержа дублированных `class="..." class="..."`
  (баг порядка атрибутов `style class` vs `class style`) — чистый результат.
- Инлайны проекта: 273 → 224 (-49 в этом батче, -197 от исходных 421).

Затронуты: accounts/*, grammar, hangul (breakdown/builder/index), lessons,
library/* (bookmarks/detail/highlights/home/index/quiz/reader/search),
progress, vocabulary/*.

## Проверка

- pytest → 125 passed
- скан `class="[^"]*"\s+class=` → 0 (после фикса)
- коммит b068e5b

## Что осталось / передано дальше

Остаток ~224 инлайна: `max-width`, `height/width` прогресс-баров (динамика),
korean font-size, tts-btn размеры, conditional badge colors — низкий приоритет,
точечно при касании файлов.
