# HANDOFF — живая доска состояния

> **Единственный мутирующий файл в `.agents/`.** Всё остальное в history/ — append-only.
>
> Протокол для ЛЮБОГО агента (opencode / Codex / Claude Code / Cursor / Gemini CLI):
> 1. **Перед началом работы** — прочитай этот файл целиком. Если есть активная задача —
>    сначала доделай её по чеклисту, не начинай свою.
> 2. **Перед остановкой** (лимиты, конец сессии, прерывание пользователем) — ОБЯЗАТЕЛЬНО
>    обнови этот файл: что сделано, что осталось, где остановился. Без этого следующий
>    агент потеряет контекст.
> 3. Задача завершена → перенеси итог в журнал (`.agents/history/`, запись NNNN),
>    очисти блок «Активная задача».
> 4. Не доверяй этому файлу слепо: проверяй факты (`git status`, pytest) — файл может
>    быть устаревшим.

---

## Снимок репозитория

- **Ветка:** main
- **Последний коммит:** d4b6878 "feat: close remaining audit lows..."
- **Незакоммичено:** нет
- **Тесты (базовая линия):** 115 passed + `ruff check .` чистый
- **Миграции:** все применены, `makemigrations --check` чист
- **CI:** GitHub Actions — ruff + миграции + pytest; красный CI у твоего
  коммита = чини до передачи задачи.
- **Аудит 2026-08 закрыт полностью** (журнал 0001–0009). known-issues.md —
  только осознанные компромиссы и прод-заметки.

## Готово, ждёт коммита

— (пусто)

## Активная задача

```
ID:            —
Название:      —
Кто начал:     —
Шаги сделаны:  —
Следующий шаг: —
Опасные места: —
```

## Очередь (кандидаты на следующую задачу)

Технический долг закрыт; дальше по желанию:
- Фичи: переучивание выученных слов (lapse-механика полноценная), лучшая/первая
  попытка квиза в статистике вместо всех попыток.
- Прод: Redis CACHES, django-environ, PostgreSQL, nginx/S3 для media,
  CSRF_TRUSTED_ORIGINS на реальный домен (см. known-issues.md).
- Контент: наполнение словаря/квизов, тесты на seed_data.
